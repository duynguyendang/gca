@baseUrl = http://localhost:8080
@project = mangle

### 1. List Projects
# List all available projects ingested into the system.
GET {{baseUrl}}/v1/projects

### 2. Get Project Summary
# Get statistics about the 'langchain' project.
GET {{baseUrl}}/v1/summary?project={{project}}

### 3. Query: Find Functions
# Find all functions in the codebase.
POST {{baseUrl}}/v1/query?project={{project}}
Content-Type: application/json

{
    "query": "triples(S, \"calls\", T), triples(File, \"defines_symbol\", S), regex(File, \"^analysis/.*\")"
}

### 4. Query: Find Structs
# Find all struct definitions.
POST {{baseUrl}}/v1/query?project={{project}}
Content-Type: application/json

{
    "query": "triples(S, \"calls\", \"fmt.Errorf\"), triples(File, \"defines_symbol\", S)"
}

### 5. Get Source Code
# Retrieve source code for a specific file/symbol.
# NOTE: Replace 'test-code/langchain/libs/text-splitters/langchain_text_splitters/base.py' with a valid ID from the query results above.
# Use a valid ID found in the "id" field of the response nodes.
GET {{baseUrl}}/v1/source?project={{project}}&id=analysis/declcheck_test.go:mustDecl

### 6. Get Symbol Search
# Search for symbols (all/top if no query provided).
GET {{baseUrl}}/v1/symbols?project={{project}}

### 7. Get Predicates
# Get the graph schema (list of available predicates).
GET {{baseUrl}}/v1/predicates?project={{project}}
